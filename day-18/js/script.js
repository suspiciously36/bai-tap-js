// var content = `
// Lorem ipsum dolor sit amet consectetur chip chip adipisicing elit. Facere labore vel sapiente magni fuga voluptas, chip chip porro nemo molestias maiores totam dignissimos! chip chip Accusantium quisquam quasi suscipit omnis nam iusto ut dolorum expedita, quam repellendus!
// `

// var keyword = "chip chip";
// var heading = `<p>Tìm kiếm với từ khóa: ${keyword}</p>`;

// document.write(heading + content + footer);

// var newContent = "";
// var position = content.indexOf(keyword);
// var count = 0;

// while (position !== -1) {
// newContent += content.slice(0, position) + `<span>${content.slice(position, position + keyword.length)}</span>`;
// content = content.slice(position + keyword.length);
// position = content.indexOf(keyword);
// count++;
// }

// var footer = `<p>Có ${count} từ khóa ${keyword} trong đoạn văn</p>`;

// newContent += content;
// document.write(heading + newContent + footer);